import { StyleSheet } from 'react-native';

import { spacing } from '@/shared/theme';

export const styles = StyleSheet.create({
  hero: { gap: spacing.sm, paddingTop: spacing.md, alignSelf: 'stretch' },
});
